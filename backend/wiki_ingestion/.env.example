# Example environment configuration for Wiki Ingestion

# ============================================================================
# WIKI CONFIGURATION
# ============================================================================

# Starting URL of the wiki to crawl (REQUIRED)
WIKI_URL=https://github.com/wso2/docs-choreo-dev/wiki

# Maximum depth to crawl (0 = only start page, 1 = start + linked pages, etc.)
WIKI_MAX_DEPTH=2

# Maximum number of pages to crawl
WIKI_MAX_PAGES=50

# Whether to fetch and process content from linked URLs (true/false)
WIKI_FETCH_LINKED=true

# Maximum number of linked URLs to process
WIKI_MAX_LINKED_URLS=50

# ============================================================================
# CHUNKING CONFIGURATION
# ============================================================================

# Target size for each chunk in characters
CHUNK_SIZE=1000

# Overlap between chunks in characters
CHUNK_OVERLAP=200

# Minimum chunk size (don't create tiny chunks)
MIN_CHUNK_SIZE=100

# ============================================================================
# HTTP REQUEST CONFIGURATION
# ============================================================================

# HTTP request timeout in seconds
REQUEST_TIMEOUT=30

# Maximum number of retry attempts for failed requests
MAX_RETRIES=3

# Backoff factor for retries (wait time = backoff_factor * (2 ^ attempt))
BACKOFF_FACTOR=0.5

# Custom user agent (optional)
# USER_AGENT=WikiCrawler/1.0 (GitHub Wiki Ingestion Bot)

# ============================================================================
# VECTOR DATABASE CONFIGURATION
# ============================================================================

# Milvus Cloud Configuration
MILVUS_URI=https://your-milvus-instance.serverless.aws-eu-central-1.cloud.zilliz.com
MILVUS_TOKEN=your_milvus_token_here
MILVUS_COLLECTION_NAME=readme_embeddings

# ============================================================================
# EMBEDDING CONFIGURATION (Optional - for embedding generation)
# ============================================================================

# Azure OpenAI endpoint
# AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com

# Azure OpenAI API key
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# Azure embedding deployment name
# AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=text-embedding-ada-002

