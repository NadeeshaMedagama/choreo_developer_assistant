# Lightweight CPU-only requirements for Docker builds
# This file uses smaller, CPU-optimized versions of ML libraries

# Core API dependencies
fastapi==0.118.0
uvicorn==0.37.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx==0.28.1
aiohttp>=3.9.0
openai==2.2.0

# Data processing
numpy>=1.24.0
pymilvus>=2.3.0
Pillow>=10.0.0
psutil>=5.9.0

# LangChain ecosystem
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0

# ML libraries (CPU-only, lighter versions)
# Note: torch will be installed separately via CPU index
# sentence-transformers will pull smaller dependencies
sentence-transformers>=2.2.2

# Google Cloud Vision
google-cloud-vision>=3.4.0

